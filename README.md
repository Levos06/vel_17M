# Transformer Model

Обученная transformer модель с архитектурой DeepSeek (RMSNorm, RoPE, SwiGLU) для генерации текста.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Использование модели

```bash
# Базовое использование
python load_model.py --prompt "The meaning of life is"

# С параметрами
python load_model.py \
    --prompt "Explain quantum physics" \
    --max-tokens 200 \
    --temperature 0.7 \
    --top-p 0.95
```

### Использование в коде

```python
from load_model import load_model, generate

# Загрузка модели
model, tokenizer, device = load_model()

# Генерация текста
text = generate(
    model, tokenizer,
    prompt="The future of AI is",
    max_new_tokens=100,
    temperature=0.8,
    top_p=0.9,
    device=device
)
print(text)
```

## 📁 Структура проекта

```
transformer/
├── models/                 # Обученные модели
│   ├── final/              # Финальная модель для использования
│   │   └── model.pt        # ← Используйте эту модель
│   └── academic_filter_model/  # Модель для фильтрации (SetFit)
│
├── training/               # Все материалы обучения (см. training/README.md)
│   ├── data/               # Данные обучения
│   ├── checkpoints/        # Чекпоинты обучения
│   ├── logs/               # Логи и метрики
│   ├── scripts/            # Скрипты обучения
│   └── docs/               # Документация обучения
│
├── load_model.py          # Скрипт для загрузки и использования модели
├── tg_bot.py              # Telegram бот (пример использования)
├── requirements.txt       # Зависимости Python
└── README.md              # Этот файл
```

## 🎯 Основные возможности

- **Генерация текста** с настраиваемыми параметрами (temperature, top-p)
- **Поддержка Apple Silicon** (MPS backend)
- **Эффективная архитектура** с RMSNorm, RoPE, SwiGLU
- **Готовые скрипты** для загрузки и использования

## 📊 Характеристики модели

- **Архитектура**: Transformer с RMSNorm, RoPE, SwiGLU
- **Параметры**: ~60M (основная модель) / ~15M (SFT версия)
- **Контекст**: 512-1024 токенов
- **Словарь**: GPT-2 tokenizer (50,257 токенов)
- **Обучение**: Pre-training + Supervised Fine-Tuning

## 🔧 Параметры генерации

- `temperature` (0.1-2.0): Контроль случайности. Низкие значения = более детерминированный текст
- `top_p` (0.0-1.0): Nucleus sampling. Фильтрует токены с низкой вероятностью
- `max_new_tokens`: Максимальное количество генерируемых токенов

## 🤗 HuggingFace

Модель доступна на HuggingFace Hub:
- **Model**: [Levos06/vel_17M](https://huggingface.co/Levos06/vel_17M)

Для загрузки модели с HuggingFace:

```python
from transformers import AutoModel, AutoTokenizer
import torch

# Загрузка модели (после публикации на HF)
model = AutoModel.from_pretrained("Levos06/vel_17M")
tokenizer = AutoTokenizer.from_pretrained("Levos06/vel_17M")
```

## 📚 Документация

- **Обучение модели**: См. `training/README.md` для подробной информации о процессе обучения
- **Архитектура**: См. `training/docs/` для технических деталей
- **Примеры использования**: См. `load_model.py` и `tg_bot.py`

## ⚙️ Требования

- Python 3.8+
- PyTorch 2.1.0+ (с поддержкой MPS для Mac)
- transformers >= 4.30.0
- numpy, tqdm

## 🐛 Устранение неполадок

### Модель не найдена

Убедитесь, что файл `models/final/model.pt` существует. Если нет, скопируйте финальный чекпоинт из `training/checkpoints/`:

```bash
cp training/checkpoints/sft_step_1200.pt models/final/model.pt
```

### Проблемы с памятью

Если модель не загружается из-за нехватки памяти:
- Используйте CPU вместо MPS: `device="cpu"`
- Уменьшите размер модели в `load_model.py`

### Медленная генерация

- Первая генерация всегда медленнее (компиляция Metal на Mac)
- Используйте меньший `max_new_tokens`
- Убедитесь, что устройство не перегревается

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🙏 Благодарности

- Архитектура вдохновлена DeepSeek и LLaMA
- Использует компоненты из современных transformer моделей (RMSNorm, RoPE, SwiGLU)
