# Оптимизированная конфигурация (~10M параметров)

## 🎯 Новые параметры модели

```python
# Модель
vocab_size = 50257
dim = 256           # было 512
n_layers = 4        # было 8
n_heads = 4         # было 8
max_seq_len = 512   # было 1024

# Обучение
batch_size = 16     # было 8
grad_accum_steps = 4
effective_batch = 64
```

## 📊 Сравнение

| Параметр | Старая модель | Новая модель | Изменение |
|----------|---------------|--------------|-----------|
| Параметры | 59.3M | ~10.5M | **÷5.6** |
| Слои | 8 | 4 | **÷2** |
| Размерность | 512 | 256 | **÷2** |
| Контекст | 1024 | 512 | **÷2** |
| Batch size | 8 | 16 | **×2** |

## ⚡ Ожидаемое улучшение скорости

**Расчет**:
- Меньше параметров (÷5.6) → ~5.6× быстрее forward/backward
- Короче контекст (÷2) → ~2× быстрее attention
- Больше batch (×2) → ~1.5× эффективнее
- **Суммарный эффект**: ~**15-20× быстрее!**

**Было**: 370 tokens/sec → **2462 часа** (103 дня)
**Ожидается**: ~5,000-7,000 tokens/sec → **~160-200 часов** (7-8 дней)

## 💾 Память

**Старая модель**:
- Модель: 237 MB
- Logits: [8, 1024, 50257] = 1.5 GB
- **Итого**: ~2.5 GB

**Новая модель**:
- Модель: 42 MB
- Logits: [16, 512, 50257] = 1.5 GB
- **Итого**: ~2.2 GB

Память примерно та же, но скорость **гораздо выше**!

## ✅ Что делать

### 1. Данные уже готовы ✓
`train.bin` остается без изменений. Контекст 512 просто означает, что модель будет читать меньшие чанки из того же файла.

### 2. Просто перезапустить обучение
```bash
python train.py
```

Модель теперь **~10.5M параметров** вместо 59M, обучение пойдет **намного быстрее**!

## 📈 Ожидаемые результаты

При 5,000-7,000 tokens/sec:
```
День 1:  Step 5,400   | Loss ~7.5
День 2:  Step 10,800  | Loss ~6.2
День 3:  Step 16,200  | Loss ~5.5
День 4:  Step 21,600  | Loss ~5.0
День 5:  Step 27,000  | Loss ~4.7
День 6:  Step 32,400  | Loss ~4.5
День 7:  Step 37,800  | Loss ~4.3
День 8:  Step 50,000  | Loss ~4.2 ✓
```

Реалистичное время: **7-8 дней** вместо 103!

## 🎓 Качество модели

**Что потеряли**:
- Меньше параметров → чуть хуже запоминание паттернов
- Короче контекст → хуже длинные зависимости

**Что сохранили**:
- Все архитектурные компоненты (RMSNorm, RoPE, SwiGLU)
- Способность генерировать связные тексты
- Демонстрация обучения

**Итог**: Модель все еще будет обучаться и генерировать осмысленные тексты, просто немного проще чем большая версия. Зато за разумное время! 🚀
